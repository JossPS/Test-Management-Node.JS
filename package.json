{
  "name": "challenge-fullstack",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix Backend-Test\" \"npm run dev --prefix Frontend-Test\"",
    "build": "npm run build --prefix Backend-Test && npm run build --prefix Frontend-Test"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}